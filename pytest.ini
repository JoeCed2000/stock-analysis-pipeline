[pytest]
markers =
    slow: long-running test (real pipeline runs, subprocess timing) — deselect with -m "not slow"
    live: hits live external APIs (yfinance, network) — manual/nightly only
    integration: requires the running backend on 127.0.0.1:8780 (auto-skips when absent)
