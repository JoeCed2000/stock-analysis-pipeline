# SKILLS.md — Stock Analysis Pipeline

## TDD
- RED→GREEN→REFACTOR. Test first, always.
- Tests: `pytest tests/ -v` from project root.

## Think in Code
- >3 read_file → write a Python script instead.

## Agentic Review
- After any code gen: check bloat, copy-paste, brittle abstractions, implicit correlations.

## Systematic Debugging
- No fixes without root cause. 4 phases: Root Cause → Pattern → Hypothesis → Implementation.

## Verification
- stat + curl + browser_console before "done".
- NTFS/WSL: Vite HMR broken for new files. Kill + restart after creating files.
- Never trust hot reload — verify with curl.
